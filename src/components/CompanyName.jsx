import React from "react";

const CompanyName = () => {
  return (
    <div className="company_name wave flex justify-center gap-8 max-w-[1920px] mx-auto mt-[64px] px-[60px] l:max-w-[960px] md:px-[30px] sm:mt-[20px] sm:px-[10px] sm:justify-center sm:gap-[30px]">
      <div className="uppercase text-[80px] md:text-[48px] sm:text-[36px] font-clash_semibold text-[#0f0f0f] textWhiteStroke text-center">
        lynn
      </div>
      <div className="uppercase text-[80px] md:text-[48px] sm:text-[36px] font-clash_semibold text-center">
        olsson
      </div>
    </div>
  );
};

export default CompanyName;
